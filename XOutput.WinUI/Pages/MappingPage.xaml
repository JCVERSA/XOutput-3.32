<Page
    x:Class="XOutput.WinUI.Pages.MappingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:XOutput.WinUI.Controls"
    xmlns:xin="using:XOutput.Devices.XInput">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mapping" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,12"/>

        <!-- Device tabs -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ListView x:Name="DeviceTabs" Grid.Row="0" SelectionChanged="DeviceTabs_SelectionChanged"
                      ItemsSource="{x:Bind Devices}" DisplayMemberPath="DisplayName"
                      SelectionMode="Single" HorizontalAlignment="Left">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>

            <!-- Two-column body: physical inputs + configure card with live preview -->
            <Grid Grid.Row="1" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.05*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <ScrollViewer>
                        <StackPanel Spacing="6">
                            <TextBlock Text="Physical Inputs" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            <ItemsControl x:Name="SourcesList"/>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Configure" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        <TextBlock Text="Select an input to configure its mapping."
                                   TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <controls:XboxControllerView x:Name="PreviewView" Width="440" Height="250"
                                                     HorizontalAlignment="Center"
                                                     LiveTarget="{x:Bind LiveTarget, Mode=OneWay}"
                                                     LiveValue="{x:Bind LiveValue, Mode=OneWay}"
                                                     Highlight="{x:Bind PreviewHighlight, Mode=OneWay}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
