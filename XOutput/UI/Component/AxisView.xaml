<UserControl x:Class="XOutput.UI.Component.AxisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XOutput.UI.Component"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:AxisViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="34" d:DesignWidth="300">
    <Grid Height="34">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="{Binding Model.Type}" VerticalAlignment="Center" Padding="0"/>
        <Grid Grid.Column="1" VerticalAlignment="Center" Height="8" Margin="6,0">
            <Border Background="{StaticResource BrushSurfaceContainerHigh}" CornerRadius="4"/>
            <Border Background="{StaticResource BrushPrimary}" CornerRadius="4" HorizontalAlignment="Stretch">
                <Border.RenderTransformOrigin>0,0</Border.RenderTransformOrigin>
                <Border.RenderTransform>
                    <ScaleTransform>
                        <ScaleTransform.ScaleX>
                            <MultiBinding Converter="{StaticResource RatioScaleConverter}">
                                <Binding Path="Model.Value"/>
                                <Binding Path="Model.Max"/>
                            </MultiBinding>
                        </ScaleTransform.ScaleX>
                    </ScaleTransform>
                </Border.RenderTransform>
            </Border>
        </Grid>
        <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="6,0,0,0"
                   Style="{StaticResource TextDataMono}" FontSize="10" Foreground="{StaticResource BrushOnSurfaceVariant}">
            <TextBlock.Text>
                <MultiBinding Converter="{StaticResource RatioScaleConverter}" StringFormat="{}{0:P0}">
                    <Binding Path="Model.Value"/>
                    <Binding Path="Model.Max"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
    </Grid>
</UserControl>
