<UserControl x:Class="XOutput.UI.Component.ButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XOutput.UI.Component"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:ButtonViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid Height="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="90"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="{Binding Model.Type}" VerticalAlignment="Center" Padding="0"/>
        <Grid Grid.Column="1" Width="40" Height="18" VerticalAlignment="Center" HorizontalAlignment="Right">
            <Border Background="{StaticResource BrushSurfaceContainerHigh}" CornerRadius="4"/>
            <Border Background="{StaticResource BrushPrimary}" CornerRadius="4" HorizontalAlignment="Stretch">
                <Border.RenderTransformOrigin>0,0</Border.RenderTransformOrigin>
                <Border.RenderTransform>
                    <ScaleTransform>
                        <ScaleTransform.ScaleX>
                            <Binding Path="Model.Value" Converter="{StaticResource BoolToDoubleConverter}"/>
                        </ScaleTransform.ScaleX>
                    </ScaleTransform>
                </Border.RenderTransform>
            </Border>
        </Grid>
    </Grid>
</UserControl>
