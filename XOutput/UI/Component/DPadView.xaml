<UserControl x:Class="XOutput.UI.Component.DPadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XOutput.UI.Component"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:DPadViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="60" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="{Binding Model.Label}" VerticalAlignment="Center" Padding="0"/>
        <Canvas Grid.Column="2" Height="50" HorizontalAlignment="Left">
            <Border Canvas.Left="6" BorderThickness="1" BorderBrush="{StaticResource BrushOutlineVariant}" CornerRadius="4" Background="{StaticResource BrushSurfaceContainerLow}">
                <Canvas Width="48" Height="48">
                    <Grid Width="48" Height="48">
                        <Path Data="M 24,0 L 24,48 M 0,24 L 48,24" Stroke="{StaticResource BrushOutlineVariant}" StrokeThickness="0.5"/>
                    </Grid>
                    <Ellipse Width="6" Height="6" Canvas.Left="{Binding Model.ValueX}" Canvas.Top="{Binding Model.ValueY}"
                             Fill="{StaticResource BrushPrimary}"></Ellipse>
                </Canvas>
            </Border>
        </Canvas>
    </Grid>
</UserControl>
