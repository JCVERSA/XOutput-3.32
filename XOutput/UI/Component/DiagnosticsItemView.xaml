<UserControl x:Class="XOutput.UI.Component.DiagnosticsItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XOutput.UI.Component"
             xmlns:diagnostics="clr-namespace:XOutput.Diagnostics"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:DiagnosticsItemViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="120" d:DesignWidth="490">
    <UserControl.Resources>
        <DataTemplate x:Key="ResultTemplate">
            <Grid Height="34" Margin="0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource TextBodyBase}">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource DynamicLanguageConverter}">
                            <Binding Path="DataContext.LanguageModel.Data" RelativeSource="{RelativeSource AncestorType={x:Type local:DiagnosticsItemView}}" />
                            <Binding Path="Type" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="12,0" Style="{StaticResource TextDataMono}">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource DynamicLanguageConverter}">
                            <Binding Path="DataContext.LanguageModel.Data" RelativeSource="{RelativeSource AncestorType={x:Type local:DiagnosticsItemView}}" />
                            <Binding Path="Value" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <!-- status pills -->
                <Border Grid.Column="2" VerticalAlignment="Center" CornerRadius="9999" Padding="10,3"
                        Background="{StaticResource BrushNavActiveBackground}"
                        Visibility="{Binding State, Converter={StaticResource EqualsToVisibilityConverter}, ConverterParameter={x:Static diagnostics:DiagnosticsResultState.Passed}}">
                    <TextBlock Foreground="{StaticResource BrushPrimary}" Style="{StaticResource TextLabelCaps}" FontSize="10">
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource DynamicLanguageConverter}">
                                <Binding Path="DataContext.LanguageModel.Data" RelativeSource="{RelativeSource AncestorType={x:Type local:DiagnosticsItemView}}" />
                                <Binding Path="State" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Border>
                <Border Grid.Column="2" VerticalAlignment="Center" CornerRadius="9999" Padding="10,3"
                        Background="{StaticResource BrushWarningSoft}"
                        Visibility="{Binding State, Converter={StaticResource EqualsToVisibilityConverter}, ConverterParameter={x:Static diagnostics:DiagnosticsResultState.Warning}}">
                    <TextBlock Foreground="{StaticResource BrushWarning}" Style="{StaticResource TextLabelCaps}" FontSize="10">
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource DynamicLanguageConverter}">
                                <Binding Path="DataContext.LanguageModel.Data" RelativeSource="{RelativeSource AncestorType={x:Type local:DiagnosticsItemView}}" />
                                <Binding Path="State" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Border>
                <Border Grid.Column="2" VerticalAlignment="Center" CornerRadius="9999" Padding="10,3"
                        Background="{StaticResource BrushErrorSoft}"
                        Visibility="{Binding State, Converter={StaticResource EqualsToVisibilityConverter}, ConverterParameter={x:Static diagnostics:DiagnosticsResultState.Failed}}">
                    <TextBlock Foreground="{StaticResource BrushError}" Style="{StaticResource TextLabelCaps}" FontSize="10">
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource DynamicLanguageConverter}">
                                <Binding Path="DataContext.LanguageModel.Data" RelativeSource="{RelativeSource AncestorType={x:Type local:DiagnosticsItemView}}" />
                                <Binding Path="State" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Border>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Border Background="{StaticResource BrushSurfaceContainer}" BorderBrush="{StaticResource BrushOutlineVariant}"
            BorderThickness="1" CornerRadius="{StaticResource CornerRadiusDefault}" Padding="16,12" Margin="4">
        <StackPanel>
            <TextBlock Text="{Binding Model.Source}" Style="{StaticResource TextTitleSm}" Margin="0,0,0,8"/>
            <ItemsControl ItemTemplate="{StaticResource ResultTemplate}" ItemsSource="{Binding Model.Results}" Focusable="False"/>
        </StackPanel>
    </Border>
</UserControl>
