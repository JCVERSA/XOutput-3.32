<UserControl x:Class="XOutput.UI.Component.InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XOutput.UI.Component"
             d:DataContext="{d:DesignInstance Type=local:InputViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="52" d:DesignWidth="300">
    <Border Background="{Binding Model.Background}" CornerRadius="8" Padding="12,8" Margin="0,2">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- status dot -->
            <Ellipse Grid.Column="0" Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,10,0"
                     Fill="{Binding Model.Device.Connected, Converter={StaticResource BoolToBrushConverter}}"/>
            <!-- name + VID/PID -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Model.DisplayName}" Style="{StaticResource TextBodyBase}" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Model.Device, Converter={StaticResource SourceToVidPidConverter}}"
                           Style="{StaticResource TextDataMono}" FontSize="10" Foreground="{StaticResource BrushOnSurfaceVariant}"/>
            </StackPanel>
            <!-- configure action -->
            <Button Grid.Column="2" Margin="8,0,0,0" Padding="12,6" Click="OpenClick">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{StaticResource IconConfigure}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding LanguageModel.Data, Converter={StaticResource LanguageConverter}, ConverterParameter='Configure'}"/>
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</UserControl>
