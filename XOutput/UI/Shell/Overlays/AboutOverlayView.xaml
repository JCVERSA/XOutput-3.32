<UserControl x:Class="XOutput.UI.Shell.Overlays.AboutOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shell="clr-namespace:XOutput.UI.Shell.Overlays"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=shell:AboutOverlayViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Path Data="{StaticResource IconInfo}" Stroke="{StaticResource BrushPrimary}"
                  StrokeThickness="1.8" StrokeLineCap="Round" Width="20" Height="20" Stretch="Uniform"
                  VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Title}" Style="{StaticResource TextHeadlineMd}" VerticalAlignment="Center"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="{Binding Text}" Style="{StaticResource TextBodyBase}" TextWrapping="Wrap" Margin="0,0,0,24"/>
        <Button Grid.Row="2" Content="{Binding LanguageModel.Data, Converter={StaticResource LanguageConverter}, ConverterParameter='Close'}"
                Width="120" HorizontalAlignment="Right" Click="CloseClick"/>
    </Grid>
</UserControl>
