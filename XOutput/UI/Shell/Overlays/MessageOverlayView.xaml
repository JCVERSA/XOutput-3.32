<UserControl x:Class="XOutput.UI.Shell.Overlays.MessageOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shell="clr-namespace:XOutput.UI.Shell.Overlays"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=shell:MessageOverlayViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="200" d:DesignWidth="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource TextHeadlineMd}" Margin="0,0,0,16"/>
        <TextBlock Grid.Row="1" Text="{Binding Message}" Style="{StaticResource TextBodyBase}" TextWrapping="Wrap" Margin="0,0,0,24"/>
        <Button Grid.Row="2" Content="{Binding LanguageModel.Data, Converter={StaticResource LanguageConverter}, ConverterParameter='Ok'}"
                Width="120" HorizontalAlignment="Right" Click="OkClick"/>
    </Grid>
</UserControl>
